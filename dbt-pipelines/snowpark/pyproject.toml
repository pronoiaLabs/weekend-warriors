[project]
name = "ww-snowpark"
version = "0.1.0"
description = "Snowpark stored procedures that dbt calls (dbt-pipelines/snowpark/)"
requires-python = ">=3.11,<3.13"
dependencies = [
    # The only runtime package the procedures declare in snowflake.yml. Pinned
    # loosely: the proc resolves the Anaconda channel's version at deploy time.
    "snowflake-snowpark-python>=1.30",
]

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.6"]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
pythonpath = ["player_bridge/src"]
testpaths = ["player_bridge/tests"]
