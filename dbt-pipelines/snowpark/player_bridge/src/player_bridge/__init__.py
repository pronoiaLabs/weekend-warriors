"""SP_PLAYER_BRIDGE: map BallDontLie and Sleeper player ids onto nflverse gsis_id.

Entry point is main.run (the procedure handler). evidence.py is pure Python
and is the one module the unit tests import; everything else needs a session.
"""

__version__ = "0.1.0"
